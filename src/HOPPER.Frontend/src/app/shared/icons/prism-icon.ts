/**
 * A prism, drawn here rather than taken from Prism Launcher.
 *
 * Their official mark is a ten-facet multicolour prism under CC BY-SA 4.0, which would drag an
 * attribution and share-alike obligation into a repo whose licence is still open, and would read as
 * mush at the 14px this is used at. This is plain geometry instead: a triangle with its front edge,
 * monochrome and following `currentColor` like the Modrinth and CurseForge glyphs next to it, so it
 * inherits the theme instead of fighting it.
 */
export const hopperPrism =
  '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" ' +
  'stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">' +
  '<path d="M12 3 21 19H3Z"/>' +
  '<path d="M12 3v16"/>' +
  '</svg>';
