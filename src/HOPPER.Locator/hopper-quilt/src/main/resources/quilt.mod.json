{
  "schema_version": 1,
  "quilt_loader": {
    "group": "ch.pianonic",
    "id": "hopper",
    "version": "1.0.0",
    "metadata": {
      "name": "HOPPER",
      "description": "Syncs the required mod set from a HOPPER server before Quilt scans for mods, and hands the downloaded jars to the loader in the same launch. Requires -Dloader.experimental.allow_loading_plugins=true - without it Quilt refuses to parse this jar at all, and hopper-fabric-1.0.0.jar should be used instead."
    },
    "intermediate_mappings": "net.fabricmc:intermediary",
    "depends": [
      {
        "id": "quilt_loader",
        "versions": ">=0.26.0"
      }
    ]
  },
  "experimental_quilt_loader_plugin": {
    "class": "ch.pianonic.hopper.HopperQuiltPlugin",
    "packages": [
      "ch.pianonic.hopper"
    ]
  }
}
