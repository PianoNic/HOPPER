{
  "name": "hopper-docs",
  "private": true,
  "type": "module",
  "description": "HOPPER documentation site (VitePress). Self-contained so a static host can build just this folder.",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
